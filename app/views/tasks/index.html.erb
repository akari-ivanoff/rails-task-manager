<h1>My Tasks</h1>
<% @tasks.each do |task| %>
  <p><%= task.completed ? "☑" : "☐" %>
  <%= link_to task.title, task_path(task) %>
  </p>
  
<% end %>
<hr>
<p>+ <%= link_to 'Add new task', new_task_path %></p>